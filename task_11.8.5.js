let xNum = 2;
let nNum = 6;

// Функция возведения числа "x" в степень "n":
const exponetiating = (x, n) => {
    let result = Math.pow(x,n);
    return result;
}

console.log(exponetiating(xNum,nNum));